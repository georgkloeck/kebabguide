<p>review creation</p>
<p><%= @review %></p>
<p><%= @restaurant.id %> -- <%= @restaurant.name %>-- <%= @user.email %></p>
<div class="alert alert-info">

  <%= flash.now[:success] = "Object successfully created"  %>
</div>
<%= form_with url: restaurant_reviews_path(@restaurant), local: true, method: :post do |f| %>
<div>


  Fillings:
  <%= link_to "+ choose filling", "#", id: "choose_filling" %>
  <p> <%= f.select("fillings", options_from_collection_for_select(Ingredient.all.select { |ing| ing.kind == "filling" }, :id, :name)) %>
  <%= render 'shared/stars_rating', index: 1  %></p>

</div>
<div class="d-inline">
 Sauce:
 <p>
  <%= f.select("sauce", options_from_collection_for_select(Ingredient.all.select { |ing| ing.kind == "sauce" }, :id, :name)) %>
  <%= render 'shared/stars_rating', index: 2 %></p>

</div>
<div class="d-inline">
Bread:
   <%= f.select("bread", options_from_collection_for_select(Ingredient.all.select { |ing| ing.kind == "bread" }, :id, :name)) %>
   <%= render 'shared/stars_rating', index: 3 %>
</div>

<div>
Salad:
    <%= f.select("salad", options_from_collection_for_select(Ingredient.all.select { |ing| ing.kind == "salad" }, :id, :name)) %>
    <%= render 'shared/stars_rating', index: 4 %>
</div>

<div>
Extra:
    <%= f.select("extra", options_from_collection_for_select(Ingredient.all.select { |ing| ing.kind == "extra" }, :id, :name)) %>
    <%= render 'shared/stars_rating', index: 5 %>
</div>
<div class="test-display">

</div>
<br>
<%= submit_tag("Save review") %>

<% end %>
<p>--------------------------------------</p>


<p>--------------------------------------</p>
<p id="demo"></p>

<p id="stars"></p>

<%#  restaurant_reviews POST   /restaurants/:restaurant_id/reviews(.:format) %>
<%# No route matches [POST] "/restaurants/1/reviews/new" %>



<script>
  const element = document.getElementById('choose_filling');
  element.addEventListener('click', () => {
    html = "<p>clicked</p>"
  const display = document.getElementById('test-display');
  display.insertAdjacentHTML('beforeend', html);
  });
</script>



